version: '3.8'

services:
  backend:
    build: ./backend
    container_name: BoltUbuntuMacmini-backend
    privileged: true
    pid: "host"
    ports:
      - "8001:8001"
    volumes:
      - ./data:/app/data
      - ./backups:/app/backups
      - ./uploads:/app/uploads
      - /var/run/docker.sock:/var/run/docker.sock:ro
    environment:
      - JWT_SECRET=raspberry_dashboard_secret_key_2024
      - DOCKER_HOST=unix:///var/run/docker.sock
    group_add:
      - "${DOCKER_GID:-999}"
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: BoltUbuntuMacmini-frontend
    ports:
      - "3050:3050"
    environment:
      - VITE_PORT=3050
      - VITE_PROXY_TARGET=http://backend:8001
    depends_on:
      - backend
    restart: unless-stopped
